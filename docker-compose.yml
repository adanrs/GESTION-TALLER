services:
  app:
    build: .
    ports:
      - "9900:9900"
    volumes:
      - taller-data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=9900
      - DB_PATH=/app/data/taller.db
    restart: unless-stopped

volumes:
  taller-data:
